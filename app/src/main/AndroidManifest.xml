<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="aara.tech.rootless_auto_user">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <meta-data android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyAJ2L2dWC-azZhByeZEw9eBVInqXut3Q2A"/>

        <meta-data
            android:name="com.razorpay.ApiKey"
            android:value="rzp_test_1nKexM5gegRysO">
        </meta-data>

        <activity android:name=".VehicleCardEditActivity"
            android:label="Edit Vehicle Details"
            android:theme="@style/AppThemeEditActivity"/>
        <activity
            android:name=".MainActivity"
            android:label="Book Now" />
        <activity android:name=".ForgetPassword" />
        <activity android:name=".SignUpActivity" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".SplashActivity"
            android:theme="@style/AppThemeEditActivity"
            android:noHistory="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>